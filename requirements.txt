Flask>=2.3
gunicorn>=21.2
requests>=2.31
numpy>=1.24
Pillow>=10.0
scikit-image>=0.21
scikit-learn>=1.3
opencv-python-headless>=4.8
scipy>=1.11

# rembg + onnxruntime are OPTIONAL. They give better background removal
# but need ~180MB of RAM/disk for their model and can be too heavy for
# Render's free tier (512MB RAM), causing crashes or slow cold starts.
# The app already falls back to a lighter corner-distance method
# automatically if these aren't installed -- so for free-tier hosting,
# just leave the two lines below commented out. Uncomment them only if
# you're on a paid plan with more memory.
# rembg>=2.0.50
# onnxruntime>=1.16
